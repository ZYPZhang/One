pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo "Selected branch: ${params.GIT_BRANCH}"
                // 在这里可以使用所选的分支执行构建操作
            }
        }
    }
}